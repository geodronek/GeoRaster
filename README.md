# GeoRaster
DLA GEODANYCH:
- Wczytaj csv [done]
- Funkcja wyświetlacją dane dla wybranego powiatu - def() / gdf.columns()  
- Wykres dla każdego województwa (matplotlib)
- Mapa dla wybranego powiatu (apka)

DLA RASTRÓW
1) PODSTAWY:
- Wczytaj GeoTIFF (ścieżka / upload)
- Wyświetl:
- rozmiar rastra (width/height)
- liczba bandów
- dtype (np. uint16/float32)
- CRS, transform, pixel size
- NoData
- Podgląd: pokaż band jako obraz (matplotlib)
- Histogram wartości
- Statystyki: min/max/mean/std dla całego rastra i dla wybranego okna (crop)

2) TBC
